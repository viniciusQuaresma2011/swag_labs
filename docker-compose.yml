version: '3.3'
services:
    test:
        network_mode: host
        image: robot
        shm_size: "256M"
        environment:
            USERNAME: Vuribi
        volumes: [
           "$PWD/resources/data/Carrinho",
           "$PWD/scripts:/scripts",
           "$PWD/reports:/reports"
        ]